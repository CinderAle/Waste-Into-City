import { AddButton, FilterButton, SidebarContainer } from './styles';

const Sidebar = () => {
    return (
        <SidebarContainer>
            <AddButton />
            <FilterButton />
        </SidebarContainer>
    );
};

export default Sidebar;
